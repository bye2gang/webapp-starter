# specs/ — 機能仕様の置き場

Loop engineering の「唯一の真実」。AI に作らせる前に、ここに「何を作るか」を書く。

- 小さな機能: `specs/<機能名>.md`（`SPEC.template.md` をコピーして使う）
- 大きな機能: `specs/001-<機能名>/` のようにフォルダで分割

**書きすぎないこと。** 重い仕様駆動ツール（Spec Kit 等）は今は使わない。1枚の Markdown で十分。
